import './main.scss'
import App from './App'
const React = window.React
const ReactDOM = window.ReactDOM

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
)
